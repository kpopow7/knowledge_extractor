services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: rag
      POSTGRES_DB: rag
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

  # Local S3-compatible API: docker compose --profile object-storage up -d minio
  # Then create a bucket and set RAG_S3_BUCKET, AWS_ENDPOINT_URL=http://minio:9000, etc.
  minio:
    image: minio/minio:latest
    profiles: ["object-storage"]
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-miniosecret}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    environment:
      RAG_STORAGE_ROOT: /data
      DATABASE_URL: postgresql://rag:rag@db:5432/rag
      REDIS_URL: redis://redis:6379/0
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      RAG_API_KEYS: ${RAG_API_KEYS:-}
      RAG_API_CORS_ORIGINS: ${RAG_API_CORS_ORIGINS:-}
      RAG_API_LOG_LEVEL: ${RAG_API_LOG_LEVEL:-INFO}
      RAG_S3_BUCKET: ${RAG_S3_BUCKET:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_ENDPOINT_URL: ${AWS_ENDPOINT_URL:-}
    volumes:
      - rag_data:/data

  worker:
    build: .
    command: python -m rag_worker.worker
    restart: unless-stopped
    depends_on:
      - db
      - redis
    environment:
      DATABASE_URL: postgresql://rag:rag@db:5432/rag
      REDIS_URL: redis://redis:6379/0
      RAG_STORAGE_ROOT: /data
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_EMBEDDING_MODEL: ${OPENAI_EMBEDDING_MODEL:-}
      RAG_INDEX_FAKE_EMBEDDINGS: ${RAG_INDEX_FAKE_EMBEDDINGS:-}
      RAG_API_LOG_LEVEL: ${RAG_API_LOG_LEVEL:-INFO}
      RAG_S3_BUCKET: ${RAG_S3_BUCKET:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_ENDPOINT_URL: ${AWS_ENDPOINT_URL:-}
    volumes:
      - rag_data:/data

volumes:
  rag_data:
  redis_data:
  pg_data:
  minio_data:
